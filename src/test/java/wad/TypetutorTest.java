package wad;

import static org.junit.Assert.assertTrue;
import org.junit.Test;

public class TypetutorTest {

    @Test
    public void testStuff() {
        // note; this is an assignment with no tests -- make sure
        // that your application works as intended before submitting it
        assertTrue(1 + 1 == 2);
    }
}
